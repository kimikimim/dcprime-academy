---
import Layout from '../components/layout/Layout.astro';

const subjects = ['전체', '수학', '영어', '과학', '국어', '탐독'];
---

<Layout title="강사소개 - 대치프라임" description="대치프라임 강사진을 소개합니다.">

  <div class="min-h-screen bg-white">

    <div class="border-b border-gray-200 bg-white">
      <div class="container mx-auto px-4 py-8">
        <h1 class="text-2xl font-bold text-gray-900">강사소개</h1>
      </div>
    </div>

    <!-- 과목 탭 -->
    <div class="sticky top-[65px] z-40 bg-white border-b border-gray-200">
      <div class="container mx-auto px-4">
        <div class="flex overflow-x-auto scrollbar-hide" id="subject-tabs">
          {subjects.map((s) => (
            <button
              data-subject={s}
              class={`subject-tab flex-shrink-0 px-6 py-4 text-sm font-medium border-b-2 transition-colors whitespace-nowrap
                ${s === '전체'
                  ? 'border-gray-900 text-gray-900'
                  : 'border-transparent text-gray-500 hover:text-gray-800'}`}
            >
              {s}
            </button>
          ))}
        </div>
      </div>
    </div>

    <div class="container mx-auto px-4 py-10">
      <div id="loading" class="text-center py-24 text-gray-400 text-sm">불러오는 중...</div>
      <div id="faculty-grid" class="hidden grid grid-cols-2 md:grid-cols-4 gap-4 max-w-4xl mx-auto"></div>
      <div id="empty-msg" class="hidden text-center py-20 text-gray-400 text-sm">해당 과목 강사 정보를 준비 중입니다.</div>
    </div>

  </div>
</Layout>

<script>
  import { supabase } from '../lib/supabase';

  const subjectColors: Record<string, string> = {
    '수학': 'text-blue-500',
    '영어': 'text-green-500',
    '과학': 'text-orange-500',
    '국어': 'text-brand',
    '탐독': 'text-purple-500',
  };

  let allFaculty: any[] = [];
  let currentSubject = '전체';

  function renderCards(subject: string) {
    currentSubject = subject;
    const grid    = document.getElementById('faculty-grid')!;
    const emptyEl = document.getElementById('empty-msg')!;

    const filtered = subject === '전체'
      ? allFaculty
      : allFaculty.filter(f => f.subject === subject);

    if (filtered.length === 0) {
      grid.classList.add('hidden');
      emptyEl.classList.remove('hidden');
      return;
    }

    emptyEl.classList.add('hidden');
    grid.classList.remove('hidden');
    grid.innerHTML = filtered.map(f => `
      <div class="bg-gray-50 rounded-xl overflow-hidden hover:shadow-md transition-shadow">
        <div class="relative h-52 bg-gray-200">
          <img
            src="${esc(f.image_url || '')}"
            alt="${esc(f.name)}"
            class="w-full h-full object-cover object-top"
            onerror="this.src='/images/faculty-placeholder-1.jpg'"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent"></div>
        </div>
        <div class="px-4 py-3">
          <p class="text-xs font-semibold mb-0.5 ${subjectColors[f.subject] ?? 'text-gray-500'}">${esc(f.subject)}</p>
          <p class="text-sm font-bold text-gray-900 mb-2">${esc(f.name)} 선생님</p>
          <p class="text-xs text-gray-400 leading-relaxed border-t border-gray-100 pt-2">${esc(f.motto ?? '')}</p>
        </div>
      </div>
    `).join('');
  }

  function esc(str: string) {
    return String(str ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  }

  async function init() {
    const { data, error } = await supabase
      .from('faculty')
      .select('*')
      .order('sort_order')
      .order('id');

    // 페이지가 이미 바뀐 경우 중단
    if (!document.getElementById('loading')) return;

    document.getElementById('loading')?.classList.add('hidden');

    if (error || !data) {
      document.getElementById('empty-msg')!.classList.remove('hidden');
      return;
    }

    allFaculty = data;
    renderCards('전체');

    // 탭 이벤트
    const tabs = document.querySelectorAll<HTMLButtonElement>('.subject-tab');
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        tabs.forEach(t => {
          t.classList.remove('border-gray-900', 'text-gray-900');
          t.classList.add('border-transparent', 'text-gray-500');
        });
        tab.classList.add('border-gray-900', 'text-gray-900');
        tab.classList.remove('border-transparent', 'text-gray-500');
        renderCards(tab.dataset.subject ?? '전체');
      });
    });
  }

  document.addEventListener('astro:page-load', init);
</script>

<style>
  .scrollbar-hide::-webkit-scrollbar { display: none; }
  .scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
</style>
